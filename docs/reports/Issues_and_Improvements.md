# 이슈 및 개선 사항 보고서
**날짜:** 2025-12-31
**프로젝트:** News Insight Pro

본 문서는 최신 통합 테스트 및 코드 리뷰를 바탕으로 식별된 이슈와 제안된 개선 사항을 정리합니다.

---

## 1. 식별된 이슈 (기능 및 아키텍처)

### 1.1. 키워드 저장의 휘발성 (Transient Keyword Storage)
*   **상태:** ✅ 해결됨
*   **설명:** `models.py`에 `keywords` 컬럼이 추가되었으며, `news.py` 엔드포인트에서 AI 분석 결과를 저장할 때 해당 필드를 업데이트하도록 구현되었습니다.

### 1.2. 프론트엔드 에러의 모호함 (Frontend Error Granularity)
*   **상태:** ⚠️ 진행 중 (기본 토스트 알림 적용됨)
*   **설명:** `react-hot-toast`를 통해 성공/실패 알림이 추가되었습니다. 단, HTTP 상태 코드별 세분화된 메시지 처리는 향후 고도화 과제로 남겨둡니다.

---

## 2. 전략적 개선 사항 (향후 로드맵) - ✅ 완료
...
---

## 3. 조치 결과
1.  [x] `backend/app/db/models.py`를 수정하여 `keywords` 컬럼 추가. (완료)
2.  [x] `503`(API 에러)과 `500`(서버 에러)을 구분하도록 `frontend` 업데이트. (토스트 알림으로 기본 대응 완료)